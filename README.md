# Makemore-pytorch
makemore is a compact, educational codebase for next-character prediction that implements and compares five architectures with the same dataset and training workflow: a simple Bigram baseline, a feed-forward MLP, a convolutional CNN, a recurrent RNN (LSTM/GRU options), and a Transformer
